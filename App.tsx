/**
 * MetalHub - Punto de entrada.
 * La app usa expo-router (main: "expo-router/entry" en package.json).
 * La navegación (Tabs + Stack) y SafeAreaProvider están en app/_layout.tsx.
 * No se muestra el mensaje por defecto de Expo; la app arranca con el layout real.
 */
export default function App() {
  return null;
}
